# Repository

A docker-compatible web challenge with a vulnerable Java Spring web application. The app acts as a repository for files, with options to add additional local or remote repos using special plugins.

## Deployment

Run ```./deploy-docker.sh``` to build and deploy the docker container, then browse to localhost port 1337.