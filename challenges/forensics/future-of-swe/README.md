# Future of SWE
This challenge is a static forensics challenge which is a USB file image. This file had several documents deleted, but the files remain on the raw image.

Contestants will need to extract the files from the image in order to complete the challenge. The flag is stored in an encrypted DOCX file, contestants will need to gather the password from the `passwords.xlsx` file first.